<UserControl x:Class="WeatherApp.UserControls.Weather.WeatherDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WeatherApp.UserControls.Weather"
             xmlns:vm="clr-namespace:WeatherApp.ViewModels.Weather"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CurrentWeatherViewModel}">
            <local:CurrentWeather />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="29" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="29" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" Grid.Row="0"
                            Grid.ColumnSpan="2" Grid.RowSpan="2"
                            Content="{Binding CurrentWeatherViewModel}" />
            <Button Grid.Row="0" Grid.Column="1" Height="24" Width="24" Command="{Binding RefreshDataCommand}">
                <Image Source="/Assets/Images/refresh.png" />
            </Button>
        </Grid>
        <ScrollViewer Grid.Row="1" Background="LightBlue" VerticalScrollBarVisibility="Auto">

        </ScrollViewer>
    </Grid>
</UserControl>
<UserControl x:Class="WeatherApp.UserControls.Weather.CurrentWeather"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WeatherApp.UserControls.Weather"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" Text="{Binding LocationName, Mode=OneWay}" />
        <TextBlock Grid.Row="1" FontSize="16" Text="{Binding LocationData, Mode=OneWay}" />
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Source="{Binding WeatherIconUrl}" VerticalAlignment="Top" />
                <Grid Grid.Column="1" Margin="5,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding Temperature}" FontSize="24" />
                        <TextBlock Text="{Binding Description}" FontSize="16"/>
                        <TextBlock Text="{Binding FeelsLikeTemperature}" FontSize="16"/>
                        <TextBlock Text="{Binding Humidity}" FontSize="16"/>
                        <TextBlock Text="{Binding Precipitation}" FontSize="16"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>